import 'package:flutter/material.dart';

class Offers_Informations_Screen extends StatelessWidget {
  const Offers_Informations_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body:  SafeArea(
        child: Text(''),
      ),
    );
  }
}