import 'package:flutter/material.dart';

class Payment_Informations_Screen extends StatelessWidget {
  const Payment_Informations_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body:  SafeArea(
        child: Text(''),
      ),
    );
  }
}