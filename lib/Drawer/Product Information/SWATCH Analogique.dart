import 'package:flutter/material.dart';

class SWATCH_Analogique_Screen extends StatelessWidget {
  
  const SWATCH_Analogique_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return   Scaffold();
  }
}