import 'package:flutter/material.dart';
class Analogue_Screen extends StatelessWidget {
  
  const Analogue_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}